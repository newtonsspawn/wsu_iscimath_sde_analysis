[workspace]
authors = ["Kenny Evans <newtonsspawn@gmail.com>"]
channels = ["conda-forge"]
name = "research_repo"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]
test = "pytest -q"
lint = "python -m compileall ."

[dependencies]
python = "3.12.*"
numpy = ">=2.3.5,<3"
matplotlib = ">=3.10.8,<4"
pandas = ">=2.3.3,<3"
time = ">=1.9,<2"
pytest = ">=9.0.2,<10"
jax = ">=0.7.2,<0.8"
